{% trans_default_domain 'FOSUserBundle' %}

        <div class="panel-heading">
            <div class="panel-title text-center">
                <h1 class="title">Academic Reminder</h1>
                <hr/>
            </div>
        </div>

    <div class="loginmodal-container">
        

    <form action="{{ path("fos_user_security_check") }}" method="post">
        {% if csrf_token %}
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        {% endif %}

        {% if error %}
            <div>{{ 'Usuário ou senha inválidos.' }}</div>
            </p>
        {% endif %}
    
        <div class="form-group">
            <label for="username">Usuário</label>
            <input type="text" id="username" name="_username" placeholder="Nome de usuário ou e-mail" value="{{ last_username }}" required="required" />
        </div>

        <div class="form-group">
            <label for="password">Senha</label>
            <input type="password" id="password" name="_password" placeholder="Senha" required="required" />
        </div>

        <div class="form-group">
            <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
            <label for="remember_me">Lembra-me</label>
        </div>

        <div class="form-group">

            <input  type="submit" id="_submit" class="btn btn-info btn-block" style="border-radius:0" name="_submit" value="LOGIN" />
        </div>

        <div class="form-group">
            <div class="login-help">
                <a href="/register" style="color: #D33724">REGISTRE-SE</a> - <a href="#">Esqueceu a senha?</a>
            </div>
        </div>

    </div>
    </form>

</div>